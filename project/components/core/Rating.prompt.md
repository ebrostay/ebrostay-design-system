One-line: Star rating — full five-star block, or `RatingInline` (single star + number) for compact rows.

```jsx
<Rating value={4.8} count={126} />
<RatingInline value={4.92} count={88} />
```
Uses the warm gold `--star` token. Empty stars use `--stone-300`.
